<li>
    {{burger_name}}

 {{#unless devoured}}
    <button class="changeDevoured" data-id="{{id}}" data-devoured="{{devoured}}">
       Devour it!
    </button>
    {{/unless}} 

{{#if devoured}}
    <button class="deleteBurger" data-id="{{id}}">
        Delete it!
    </button>

    {{/if}} 


</li>

